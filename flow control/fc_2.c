#include <stdio.h> 
  
int main() 
{ 
    // printf會回傳打印的字符数
    if (printf("geeks")) // 5
        switch (printf("for")) // 3 > no case 3 > 所以switch內部的區段不會被執行
            while (printf("geeks")) 
                return 0; 

        printf(" after switch");
} 